---
title: "ViQ: Text-Aligned Visual Quantized Representations at Any Resolution"
title_zh: ViQ：任意分辨率下的文本对齐视觉量化表示
authors: "Xumin Yu, Zuyan Liu, Zhenyu Yang, Yuhao Dong, Shengsheng Qian, Jiwen Lu, Han Hu, Yongming Rao"
date: 2026-06-25
pdf: "https://arxiv.org/pdf/2606.27313v2"
tags: ["query:vit"]
score: 9.0
evidence: 文本对齐的视觉量化表示
tldr: "现有视觉离散表示难以兼顾语义与细节。ViQ提出两阶段量化学习框架，通过文本对齐预训练增强语义，并采用近端表示学习与位置感知头量化实现任意分辨率处理。在多模态任务中，ViQ以低维离散特征达到甚至超越连续高维编码器的性能，同时保持低层重建精度，并带来20%-70%的训练加速。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.27313v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1442, \"height\": 471, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27313v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1444, \"height\": 733, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27313v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1018, \"height\": 762, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27313v2/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1428, \"height\": 352, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27313v2/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 457, \"height\": 123, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27313v2/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 455, \"height\": 121, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27313v2/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1439, \"height\": 480, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27313v2/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1446, \"height\": 522, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.27313v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1456, \"height\": 646, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27313v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1450, \"height\": 591, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27313v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 459, \"height\": 181, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27313v2/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 466, \"height\": 167, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27313v2/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 477, \"height\": 188, \"label\": \"Table\"}]"
motivation: 解决离散视觉表示中语义信息与细节保真度难以兼得的问题，并支持原生分辨率输入。
method: 两阶段量化学习：先通过文本对齐预训练使编码器获得丰富语义；再通过近端表示学习和位置感知头量化实现特征离散化与分辨率自适应。
result: "多模态任务上性能媲美甚至超越SOTA连续高维编码器，同时低层重建精确，训练效率提升20%-70%。"
conclusion: ViQ作为统一离散视觉表示，兼顾语义与细节，支持任意分辨率，显著提升多模态训练效率。
---

## 摘要
文本与视觉的统一表示是一个自然的追求，因为它能够实现更简单的多模态建模和更高效的训练。然而，将图像表示为与文本相同的离散信号不可避免地引入了严重的信息损失。现有工作难以在离散表示中平衡低层细节和高层语义：面向重建的表示通常缺乏语义信息，而语义更强的特征则往往遭受严重的细节损失。我们提出了ViQ，一个视觉量化表示框架，旨在平衡离散表示中的语义与细节，同时支持原生分辨率输入，从而使其能够作为任意视觉输入的通用统一离散表示。我们的方法将量化学习分为两个阶段：文本对齐预训练和特征离散化。通过文本对齐预训练，我们利用预训练语言模型增强了视觉编码器的语义丰富监督，并使其能够处理原生分辨率的视觉输入。在离散化过程中，我们提出了一种邻近表示学习策略，逐步压缩特征空间，并结合位置感知的逐头量化机制，实现了对任意分辨率的灵活处理。在多模态任务上的大量实验表明，与具有连续高维视觉特征的最先进多模态视觉编码器相比，ViQ在保持低层重建高精度的同时，取得了具有竞争力的性能。我们还表明，使用视觉量化表示进行多模态训练极大地提高了效率，在不同的基础大语言模型和训练方案下，实现了20%至70%的加速。

## Abstract
A unified representation for text and vision is a natural pursuit, as it enables simpler multimodal modeling and more efficient training. However, representing images as discrete signals in the same way as text inevitably introduces severe information loss. Existing work struggles to balance low-level details and high-level semantics in discrete representations: reconstruction-oriented representations often lack semantic information, whereas semantically stronger features typically suffer from severe loss of detail. We present ViQ, a Visual Quantized Representations framework, which is designed to balance semantics and details in discrete representations while supporting inputs at native resolutions, thereby enabling it to serve as a unified and general discrete representation for arbitrary visual inputs. Our approach structures quantization learning into two stages: text-aligned pre-training and feature discretization. With text-aligned pre-training, we enhance the visual encoder semantic-rich supervision from the pretrained language model and enable it to process native-resolution visual inputs. During discretization, we propose a proximal representation learning strategy to progressively compact the feature space, along with a position-aware head-wise quantization mechanism that enables flexible processing of arbitrary resolutions. Extensive experiments on multimodal tasks demonstrate that ViQ achieves competitive performance compared to state-of-the-art multimodal vision encoders with continuous and high-dimensional visual features, while maintaining high precision in low-level reconstruction. We also show that multimodal training with visual quantized representations largely improves efficiency, yielding up to 20\%-70\% acceleration with different base LLMs and training recipes.

---

## 论文详细总结（自动生成）

# 论文总结：ViQ: Text-Aligned Visual Quantized Representations at Any Resolution

## 1. 核心问题与研究动机
- **背景**：多模态大语言模型（MLLM）需要高质量、统一的视觉表示。现有方法大多采用连续视觉编码器（如CLIP、SigLIP），其连续特征与文本的离散标记存在内在不匹配，且高维特征带来较大训练计算负担。
- **核心问题**：离散视觉表示虽可统一模态，但现有技术难以同时保留低层细节（如像素级信息）和高层语义（如目标识别、场景理解）。重建导向的量化表示缺乏语义，语义强的特征则细节损失严重。
- **整体目标**：提出ViQ，一种能在任意分辨率下生成文本对齐的离散视觉表示的框架，旨在平衡语义与细节，并作为统一的视觉表示用于多模态任务。

## 2. 方法论
- **两阶段训练框架**：
  - **Stage 1: 文本对齐预训练（Text-Aligned Pre-training）**
    - 基于SigLIP2-g初始化，替换固定位置编码为可适应任意分辨率的位置嵌入（类似NaViT）。
    - 利用语言模型（Qwen2.5-0.5B）的文本损失进行多模态对齐：\( L_{\text{text}} = \text{CrossEntropy}(\text{LLM}(\text{ViQ}(I), T), A) \)。
    - 自蒸馏损失：固定分辨率教师模型（原始SigLIP2-g）监督学生模型的语义标记余弦相似度，防止遗忘预训练知识：\( L_{\text{distill}} = 1 - \cos(z_s^{\text{sem}}, z_t^{\text{sem}}) \)。
    - 渐进式训练：从低分辨率（≤384²）逐步过渡到原生分辨率（768²），打包多模态数据以提高效率。
  - **Stage 2: 视觉量化表示学习（Visual Quantized Representation Learning）**
    - 分为两子阶段：
      - **Stage 2-1: 邻近表示学习（Proximal Representation Learning）**  
        - 通过瓶颈层将高维特征（C=1536）压缩至中间维D=128，并使用L∞范数投影到超立方体表面（∥f₁∥∞=1），逐步限制特征空间复杂度。
        - 保留文本损失和自蒸馏损失，并添加低层重建损失：\( L_{\text{recon}} = \text{NLL}(\hat{f}, \text{Encoder}(x)) \)（基于预训练VAE编码器的潜在损失，实际为MSE）。
      - **Stage 2-2: 量化训练（Quantization Training）**  
        - 用多头部有限标量量化（FSQ）替换L∞范数：将特征进一步压缩至d=6维，量化级别L=[8,8,8,5,5,5]，码本大小64,000。
        - 引入“头部扩展”：每个视觉块（patch）通过线性层扩展为2×2=4个子标记，在子标记内进行多头自注意力，然后量化，最后融合恢复每块一个标记。
        - 2D旋转位置编码（RoPE）注入空间信息：\( \tilde{f}_m = f_m \odot e^{i(h\theta_h + w\theta_w)} \)，支持任意分辨率。
    - 总损失：\( L_{\text{total}} = \lambda_{\text{text}} L_{\text{text}} + \lambda_{\text{distill}} L_{\text{distill}} + \lambda_{\text{recon}} L_{\text{recon}} \)。

## 3. 实验设计
- **多模态理解任务**：
  - 基准（9个）：MMStar, MMMU, SimpleVQA, InfoVQA, TextVQA, DocVQA, OCRBench, AI2D, ChartQA。
  - 对比方法：
    - 连续编码器：OAI-CLIP-L, SigLIP2-g, DINOv2-g, OryxViT, AIMv2-H, InternViT-2.5（含6B变体）。
    - 离散编码器：QLIP, UniTok。
  - 训练数据：固定200万样本子集（来自LLaVA-OneVision），使用LLaVA-NeXT的“任意分辨率”流水线进行公平对比。
- **重建质量任务**：
  - ImageNet-1K验证集（256×256）。
  - 对比方法：连续（SD-VAE, FLUX-VAE, Qwen-Image, Cosmos-CI, Wan2.2）和离散（Cosmos-DI, Show-o, LlamaGen, MUSE-VL, Open-MAGVIT2, QLIP-B, UniTok）。
  - 评估指标：PSNR, SSIM, rFID。
- **效率实验**：
  - 与SigLIP2-g对比训练时间（前向时间、步时间），使用不同LLM大小（Qwen2.5 0.5B~7B）和序列长度（4k, 16k）。
- **消融实验**：
  - 邻近表示设计：瓶颈+L2/L∞、渐进正则化。
  - 量化方法对比：SimVQ, FSQ, LFQ, vanilla VQ。
  - 位置编码：无位置、可学习位置嵌入、RoPE。
  - 损失组合：文本损失、自蒸馏、重建损失的消融。
  - 重建损失类型：MSE+LPIPS、DiT（冻结/未冻结）、VAE潜在损失。
- **附加分析**（附录）：
  - 规模匹配对比（ViQ-0.4B）。
  - 量化方法额外比较。
  - 编码器内存和吞吐量分析。

## 4. 资源与算力
- **硬件**：
  - Stage 1（文本对齐预训练）：128张 NVIDIA A100 GPU。
  - Stage 2（量化训练）：256张 NVIDIA A100 GPU。
- **数据规模**：
  - Stage 1：约3B视觉-语言tokens（低分辨率）+ 约3B tokens（高分辨率）。
  - Stage 2-1：1B视觉-语言tokens。
  - Stage 2-2：约30B视觉-语言tokens。
- **训练时间**：文中未明确给出具体时长，但可推断为大规模分布式训练。

## 5. 实验数量与充分性
- **实验数量**：大量实验覆盖了多模态理解（9个基准 × 多种编码器）、重建（16个对比方法）、效率（4个LLM规模 × 2种序列长度）、消融（6个表格，每个包含多个子实验）。
- **充分性**：
  - 对比方法全面：包括主流连续和离散编码器，并确保训练数据和协议一致。
  - 消融实验覆盖核心设计：邻近表示、量化方法、位置编码、损失组合、重建损失类型。
  - 补充实验：规模匹配对比、量化方法深入分析、内存吞吐分析。
- **客观性与公平性**：
  - 使用统一训练数据（200万样本），采用相同评估工具（LMMs-Eval）。
  - 对固定分辨率编码器采用LLaVA-NeXT的“任意分辨率”适配，保证公平。
  - 实验报告标准指标（平均分），并讨论局限性。

## 6. 主要结论与发现
- **性能**：在Qwen2.5-1.5B和7B骨干下，ViQ的平均得分分别为57.2和63.9，超越了所有此前离散编码器，并匹配或超越了大多数连续编码器（如InternViT-2.5-6B得分57.0/63.8）。在OCR、文档、图表等细节密集型任务上增益显著。
- **重建质量**：rFID达到0.62（离散编码器中排名第二，仅次于UniTok的0.37），PSNR 22.73，SSIM 0.66，表明在语义对齐下仍保持较好细节。
- **效率**：使用预计算的ViQ离散码，训练时前向时间加速20%-78%，整体步时间加速20%-40%，具体取决于LLM大小和序列长度。
- **关键洞察**：
  - 渐进式特征空间正则（L∞）对提升量化后性能至关重要。
  - 无参数学习的FSQ优于需要学习码本的SimVQ/LFQ等。
  - RoPE位置编码显著优于可学习位置嵌入。
  - VAE潜在重建损失在计算成本（1.3倍）和性能之间取得最佳平衡。

## 7. 优点
- **方法设计**：
  - 两阶段训练有效分离语义对齐与离散化任务，避免单一阶段的信息损失。
  - 邻近表示（L∞正则）和逐步压缩降低了量化对特征的扰动。
  - 头部扩展+内部注意力增强了量化表示容量，且不增加下采样率。
  - 支持原生分辨率输入，适应性强。
- **实验设计**：
  - 对比公平，覆盖多种连续/离散编码器。
  - 消融实验系统完整，验证了每个组件的必要性。
  - 效率分析放在实际训练场景下，体现实用价值。
- **性能亮点**：在低维离散表示（d=6）下达到与高维连续特征相当的语义理解能力，同时保持良好重建质量，实现统一表示。

## 8. 不足与局限
- **实验覆盖**：
  - 未验证与超大LLM（如70B+）的集成效果，缺乏大规模真实场景验证。
  - 重建质量（PSNR/SSIM）仍低于连续分词器（如SD-VAE, FLUX-VAE），且rFID不及UniTok（因UniTok使用了更强的直接重建监督，但语义对齐较弱）。
  - 在OCRBench等超细节任务上，ViQ得分（636）低于InternViT-2.5-6B（690）和InternViT-2.5（681），表明离散量化仍带来高频信息损失。
- **偏差风险**：
  - 预训练数据依赖：模型性能受限于训练数据的质量和多样性，在极端域外场景中泛化可能受到影响。
  - ViQ基于SigLIP2-g初始化，若教师模型存在偏见，可能继承。
- **应用限制**：
  - 编码器本身吞吐量略低于原始SigLIP2-g（约慢15%），尽管离线提取后可摊销。
  - 最佳码本大小64,000相对较小，进一步增大码本对非学习型量化提升有限。
- **其他**：
  - 附录中未讨论不同分辨率下性能的详细变化（仅说明支持任意分辨率）。
  - 训练数据量较大，算力需求高，可能限制复现。

（完）
