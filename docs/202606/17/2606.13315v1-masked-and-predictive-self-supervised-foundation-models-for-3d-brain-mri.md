---
title: Masked and Predictive Self-Supervised Foundation Models for 3D Brain MRI
title_zh: 面向三维脑部MRI的掩码与预测式自监督基础模型
authors: "Esra Ergün, Hersh Chandarana, Dan Sodickson, Gözde Ünal"
date: 2026-06-11
pdf: "https://arxiv.org/pdf/2606.13315v1"
tags: ["query:vit"]
score: 8.0
evidence: 掩码自编码器用于3D脑MRI自监督学习
tldr: 现有MRI基础模型主要关注分割，疾病检测研究不足。本文探索MAE和JEPA两种自监督范式，为MAE引入光谱域重建损失增强细粒度结构感知，为JEPA集成方差-协方差正则化促进解耦表示。在五个疾病检测任务上，MAE+光谱监督持续最优，光谱正则化利于高频结构任务，协方差正则化利于多解耦特征任务。揭示了自监督目标的设计对下游任务结构的依赖性，为医学影像基础模型提供指导。
source: arxiv
selection_source: carryover_cache
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.13315v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1283, \"height\": 816, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13315v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 728, \"height\": 780, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13315v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1409, \"height\": 504, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13315v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1299, \"height\": 673, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13315v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1405, \"height\": 486, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.13315v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1430, \"height\": 1309, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.13315v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1432, \"height\": 1030, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.13315v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1431, \"height\": 1044, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.13315v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1434, \"height\": 1056, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.13315v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1433, \"height\": 1025, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.13315v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1344, \"height\": 334, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.13315v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1124, \"height\": 638, \"label\": \"Table\"}]"
motivation: 现有MRI基础模型对疾病检测缺乏系统研究，需对比不同自监督预训练范式的影响。
method: 采用MAE和JEPA两种范式，分别引入光谱域重建损失和方差-协方差正则化，在异质单对比MRI上做对比无关预训练。
result: MAE+光谱监督在五个疾病检测任务中一致最优，光谱正则化利于高频结构任务，协方差正则化利于特征解耦任务。
conclusion: 自监督目标编码特定偏差，下游收益取决于任务结构，指导医学影像基础模型设计。
---

## 摘要
自监督基础模型在医学影像中显示出强大潜力。然而，现有的MRI基础模型研究主要强调分割和密集预测任务，而针对基于MRI的疾病检测的自监督基础模型的系统研究仍有限。在这项工作中，我们研究了两种主要的基于MRI疾病检测的自监督预训练范式：通过掩码自编码器（MAE）的基于重建的学习，以及通过联合嵌入预测架构（JEPA）的预测表示学习。我们通过为MAE引入一种新颖的频谱域重建损失来增强对精细解剖结构的敏感性，并通过在我们的JEPA框架中集成方差-协方差正则化（VCR）来鼓励去相关的潜在表示，从而研究辅助目标的作用。我们的模型在对比无关的设置中，在异构单对比MRI体数据上进行预训练，无需模态拼接。在五个下游疾病检测任务中，我们的结果凸显了自监督目标设计对医学基础模型预训练的重要性，表明每个目标的下游收益由其与任务结构的相关性决定。具体来说，当下游判别信号以强高频解剖结构为特征时，频谱正则化带来最大改进；而当判别信息跨越多个去相关特征维度时，协方差正则化最有益。带有频谱域监督的MAE在基于MRI的疾病检测中持续实现优越的下游性能。这些发现表明，医学影像中的自监督目标编码了特定的偏见，其下游收益从根本上取决于任务的结构。

## Abstract
Self-supervised foundation models have shown strong promise in medical imaging. However, existing MRI foundation-model studies have primarily emphasized segmentation and dense prediction tasks, while systematic investigation of self-supervised foundation models for MRI-based disease detection remains limited. In this work, we investigate two major self-supervised pretraining paradigms for MRI-based disease detection: reconstruction-based learning via Masked Autoencoders (MAE) and predictive representation learning via Joint Embedding Predictive Architectures (JEPA). We study the role of auxiliary objectives by introducing a novel spectral-domain reconstruction loss for MAE to enhance sensitivity to fine-grained anatomical structure, and by integrating variance--covariance regularization (VCR) within our JEPA framework to encourage decorrelated latent representations. Our models are pretrained on heterogeneous single-contrast MRI volumes in a contrast-agnostic setting, without modality concatenation. Across five downstream disease detection tasks, our results highlight the importance of self-supervised objective design for medical foundation model pretraining, demonstrating that the downstream benefit of each objective is determined by its relevance to the task's structure. Specifically, spectral regularization yields the largest improvements when the downstream discriminative signal is characterized by strong high-frequency anatomical structures, while covariance regularization is most beneficial when discriminative information spans multiple decorrelated feature dimensions. MAE with spectral-domain supervision consistently achieves superior downstream performance for MRI-based disease detection. These findings suggest that self-supervised objectives in medical imaging encode specific biases, and their downstream benefit is fundamentally conditioned on the task's structure.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
**研究动机**：现有 MRI 基础模型研究主要聚焦分割等密集预测任务，对疾病检测（如阿尔茨海默病、肿瘤分级、孤独症）的系统研究不足。同时，自监督预训练的目标设计（如图像重建 vs. 潜在预测）如何影响下游任务的迁移能力尚不明确。
**整体含义**：本文系统比较了两种主流自监督范式（**MAE** 和 **JEPA**）在 3D 脑 MRI 疾病检测中的表现，并引入领域驱动的辅助目标（**频谱域重建损失** 和 **方差-协方差正则化 VCR**），揭示了自监督目标的“任务结构性依赖”——即不同目标编码的偏置只有在下游任务的统计特性与其匹配时才能带来最大收益。

### 2. 论文提出的方法论
**核心思想**：  
- **MAE**：基于像素重建，通过掩码高比例（75%）的 3D 体素块，用轻量解码器重建完整体数据。  
- **JEPA**：基于潜在预测，学生编码器处理可见块，通过预测器预测教师编码器（EMA 更新）对掩码区域的潜在表示，不重建像素。  
- 为 MAE 引入 **频谱损失（SL-MAE）**：对重建和原始体数据做 3D FFT，取对数幅度后施加高通滤波器，计算 MSE，增强对高频解剖结构的敏感性。  
- 为 JEPA 集成 **方差-协方差正则化（VCR-JEPA）**：在联合上下文和目标编码器表示上施加方差约束（防止坍塌）和协方差约束（鼓励去相关特征维度）。  

**关键技术细节**：
- 输入：单对比 3D MRI（T1/T2/FLAIR/T2*），保持原生方向，裁剪至 160×160 固定深度插值，不进行空间归一化或强度均一化。  
- 掩码策略：3D 空间-深度块掩码（8³ 块大小），随机放置直到 75% 掩码。  
- 架构：ViT-Base 3D 骨干（patch size 8³），MAE 使用轻量解码器，JEPA 使用预测器。  
- 预训练损失：MAE: L_MAE (MSE) + λ_FFT * L_spec（频谱损失每 n 步计算一次）；JEPA: L_JEPA (L1 潜在预测) + α_var*L_var + β_cov*L_cov。  
- 频谱损失采用摊销策略：每 10 步计算一次，梯度平滑影响后续步骤，降低计算开销。

### 3. 实验设计
**数据集**：
- **预训练**：7 个数据集（ADNI, NACC/SCAN, PPMI, OASIS-3, IXI, MOOD, BraTS-2024），共 58,781 个体积，8,769 个受试者。  
- **下游评估**：4 个数据集（ADNI, NACC/SCAN, UCSF-PDGM, ABIDE），共 5 个任务（AD vs NC, MCI vs NC, 二元/多元肿瘤分级, ASD vs NC）。ABIDE 和 UCSF 未出现在预训练中，用于跨域评估。  

**Benchmark 与对比方法**：
- 基线：标准 MAE、标准 JEPA。  
- 变体：SL-MAE（带频谱损失）、VCR-JEPA（带方差-协方差正则化）。  
- 外部对比：BrainFound（基于 DINOv2）、全监督 ResNet50-3D（少量样本实验）。  
- 评估策略：微调（全参数）和冻结（仅训练分类头），5 折交叉验证（ADNI）或单次分裂（其他）。  
- 消融实验：有无频谱损失、有无 VCR；少量样本（16/32/64 张/类）。  

**实验充分性**：覆盖 5 个疾病检测任务，使用微调+冻结两种设置，包括跨任务和跨域迁移，进行消融、少量样本、聚类分析（t-SNE + Silhouette/NMI），并与最近相关工作对比。实验规模较大，设计较为客观公平（严格受试者级数据分割并公开）。

### 4. 资源与算力
- **预训练**：4 块 NVIDIA A100 GPU，批量大小 196，AdamW 优化器。  
- **MAE 预训练**：300 epochs（学习率 1e-4 线性预热至 5e-4，余弦衰减至 5e-5）。  
- **JEPA 预训练**：300 epochs（学习率 1e-4 线性预热至 2e-4，余弦衰减至 1e-6）。  
- **下游微调**：2 块 NVIDIA A100 GPU。  
- 论文未报告具体训练时长（小时/天），但给出了算法细节。

### 5. 实验数量与充分性
- **主实验**：表 2-5 分别报告了 MAE/SL-MAE、JEPA/VCR-JEPA 在 5 个任务上的微调与冻结结果（含 5 折交叉验证均值标准差）。  
- **消融**：频谱损失 vs 无；VCR vs 无。  
- **少量样本**：图 5 展示了 16/32/64 样本下的 AUC 对比（SL-MAE、VCR-JEPA 与 ResNet50-3D）。  
- **可视化**：t-SNE 投影（图 4）及聚类指标（表 7）。  
- **外部对比**：表 6 与 BrainFound 在 ADNI/UCSF/NACC 任务上的 AUC 比较。  
- **综合评估**：实验覆盖了多种疾病、多模态对比、多种下游设置（微调/冻结、少量样本），且控制了数据泄漏；实验设计比较充分，结果呈现清晰。不足之处在于未与 DINOv3 等最新方法对比。

### 6. 论文的主要结论与发现
- **MAE 整体优于 JEPA**：在大多数疾病检测任务中，MAE（尤其 SL-MAE）的 AUC 高于 JEPA（尤其 VCR-JEPA）。  
- **频谱损失（SL-MAE）** 在 **具有强高频结构** 的任务（如 AD vs NC、肿瘤分级）中带来显著提升，而在高频信号较弱（如 NC vs MCI、ASD）时帮助有限；验证了高频偏置与任务结构的匹配。  
- **VCR（VCR-JEPA）** 在 **判别信号跨多个去相关特征维度** 的任务（如 AD vs NC、肿瘤分级）中受益，而在 MCI/ASD 这类信号集中或弱表达的任务中无优势甚至退化。  
- **自监督目标并非通用**：不同目标编码不同偏置，下游收益取决于任务统计特性；因此设计自监督目标时应考虑下游任务的结构。  
- **少量样本**：SL-MAE 和 VCR-JEPA 在 16 样本下显著优于全监督 ResNet50-3D，表明 SSL 预训练有利于低标注设置。  
- **冻结 vs 微调**：在某些困难任务上冻结反而优于微调（如 NACC MCI、ABIDE），可能是微调过拟合或表示扭曲。

### 7. 优点
- **系统比较两大范式**：首次在受控条件下并排对比 MAE 和 JEPA 在 3D 脑 MRI 疾病检测上的表现。  
- **领域驱动的辅助目标**：频谱损失增强高频细节（对肿瘤、AD 有益），VCR 鼓励去相关特征（对多源病变量有益），两种目标均具有明确动机。  
- **对比无关、异质预处理**：不依赖空间归一化或强度均一化，提升模型对跨协议数据的鲁棒性。  
- **公开数据分割**：严格受试者级划分，并公开所有 splits，促进可重复性。  
- **多维度评估**：包括微调/冻结、少量样本、聚类分析、t-SNE 可视化，实验设计全面。

### 8. 不足与局限
- **架构规模有限**：仅使用 ViT-Base，未探索更大模型（如 ViT-Large/Huge）。  
- **缺乏与最新自监督框架的对比**：未与 DINOv3、I-JEPA 等新方法比较，对比仅停留在 BrainFound。  
- **任务范围局限**：仅评估脑 MRI 疾病检测（分类），未测试分割任务或其他模态（CT、超声）。  
- **JEPA 性能较低**：JEPA 可能因 3D 适配与调参不足表现偏弱，其潜力未充分发挥。  
- **频谱损失计算成本**：尽管采用摊销策略，仍需每 10 步计算 3D FFT，预训练开销仍高于标准 MAE。  
- **部分任务结果偏低**：NC vs MCI 和 ABIDE 的 AUC 约 0.6-0.7，表明现有自监督表示对这些细微疾病变化的区分能力仍有限。  
- **跨域验证不足**：虽然 UCSF/ABIDE 为留出，但未在更多独立外部数据集上验证（如不同种族、不同 MR 厂商）。

（完）
