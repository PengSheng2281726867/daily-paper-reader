---
title: "ScalePredictor: Instance-aware Scale Learning for Accurate Quantization of Vision Transformers"
title_zh: "ScalePredictor: 实例感知的尺度学习用于视觉Transformer的精确量化"
authors: "Changjun Li, Runqing Jiang, Lian Xu, Ye Zhang, Qingyong Hu, Yulan Guo"
date: 2026-06-20
pdf: "https://arxiv.org/pdf/2606.21947v1"
tags: ["query:vit"]
score: 8.0
evidence: 提出针对视觉Transformer的动态量化方法，直接相关ViT高效部署
tldr: 视觉Transformer部署边缘设备面临高计算量挑战，后训练量化（PTQ）可压缩模型，但现有静态量化忽略不同实例激活分布差异，导致次优。本文提出ScalePredictor动态量化框架，发现浅层激活分布范围与深层最优尺度隐含关联，通过高效范围提取和Taylor多项式投影模块同时生成所有量化尺度。在ImageNet上以极小计算开销持续超越先前PTQ方法，实现更优精度-效率权衡。该工作为ViT量化提供实例感知高效方案，推动边缘部署。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.21947v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1273, \"height\": 601, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21947v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1272, \"height\": 493, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21947v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1261, \"height\": 626, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21947v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1237, \"height\": 500, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.21947v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1274, \"height\": 1303, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21947v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1261, \"height\": 1410, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21947v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 512, \"height\": 477, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21947v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 462, \"height\": 180, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21947v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 928, \"height\": 264, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21947v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 670, \"height\": 176, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21947v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 526, \"height\": 176, \"label\": \"Table\"}]"
motivation: 揭示静态量化忽略实例间激活分布差异的局限，基于浅层-深层尺度相关性提出动态量化。
method: 设计范围提取获得浅层鲁棒统计量，经Taylor多项式投影生成所有量化尺度，避免即时校准。
result: 在ImageNet上以极小额外计算超越先前PTQ方法，取得更优精度-效率权衡。
conclusion: ScalePredictor为ViT量化提供实例感知且高效的学习框架，促进边缘部署。
---

## 摘要
视觉Transformer在许多领域取得了显著成功，但由于其计算需求巨大，在边缘设备上的部署仍然具有挑战性。训练后量化（PTQ）通过使用小型校准集以最小的训练开销压缩模型，提供了一种有吸引力的解决方案。然而，大多数现有的PTQ工作采用静态量化范式，统一应用于所有实例。由于自然图像具有显著的多样性，激活分布在不同样本之间变化很大，使得这些方法本质上是次优的。在本文中，我们提出了ScalePredictor，一种用于ViTs精确高效量化尺度学习的动态量化框架。我们首先揭示了浅层激活的分布范围与深层最优尺度之间的隐藏相关性。基于此，我们开发了一种尺度学习机制，该机制集成了高效的范围提取方法，以在浅层阶段捕获稳健的范围统计信息，然后将其输入到基于泰勒多项式的尺度投影模块中，同时生成所有量化尺度。凭借多项式逼近的效率，ScalePredictor引入了微不足道的计算开销，同时避免了昂贵的即时校准。在ImageNet上的大量实验表明，ScalePredictor始终优于先前的PTQ方法，实现了更有利的精度-效率权衡。代码和额外结果见补充材料。

## Abstract
Vision Transformers have achieved remarkable success in many fields, yet their deployment on edge devices remains challenging due to their substantial computational demands. Post-Training Quantization (PTQ) offers an attractive solution by compressing models using a small calibration set with minimal training overhead. However, most existing PTQ works adopt a static quantization paradigm that is uniformly applied to all instances. Given the substantial diversity of natural images, the activation distributions vary significantly across samples, making these methods inherently suboptimal. In this paper, we propose ScalePredictor, a dynamic quantization framework for accurate and efficient quantization scale learning of ViTs. We first reveal a hidden correlation between the distribution range of shallow-layer activations and the optimal scales of deeper layers. Based on this, we develop a scale learning mechanism that integrates an efficient range extraction approach to capture robust range statistics at the shallow stage, which are then fed into a Taylor-motivated polynomial scale projection module to generate all quantization scales simultaneously. With the efficiency of polynomial approximation, ScalePredictor introduces insignificant computational overhead while avoiding costly just-in-time calibration. Extensive experiments on ImageNet demonstrate that ScalePredictor consistently outperforms prior PTQ methods, achieving a more favorable accuracy-efficiency trade-off. Code and additional results are shown in the supplementary materials.

---

## 论文详细总结（自动生成）

# 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：视觉Transformer（ViT）在边缘设备部署面临高计算量挑战，后训练量化（PTQ）是有效压缩方案。但现有PTQ方法大多采用**静态量化范式**，对所有输入样本使用相同的量化参数。自然图像存在显著多样性，不同实例的激活分布差异巨大，静态量化导致次优的精度-效率权衡。
- **研究动机**：作者观察到浅层（Patch Embedding输出）激活的分布范围与深层各层激活的最优量化尺度之间存在**强相关性**。基于此启发，希望设计一种**实例感知的动态量化**方案，既能根据每张输入图像生成个性化尺度，又避免传统动态量化（逐层运行时全张量归约）产生的高延迟开销。

# 2. 方法论：核心思想、关键技术细节

### 2.1 整体框架：ScalePredictor
- **核心思想**：从浅层（Patch Embedding输出）提取一个鲁棒的标量范围`r(x)`，然后利用一个轻量级的多项式预测器（Taylor-Motivated Polynomial Predictor），同时预测后续所有量化层的实例特定尺度`s_l(x)`。整个过程只需在推理开始时执行一次范围提取，避免了逐层的运行时归约。

### 2.2 关键技术组件
#### ① 鲁棒范围提取（Robust Range Extraction）
- **输入**：Patch Embedding输出张量 `x_0 ∈ R^(N×C)`。
- **步骤**：
  1. 将扁平化后的张量分为 `K` 个不重叠的块（chunk）。
  2. 计算每个块的局部最大值和最小值。
  3. 对所有块的极值取平均，得到鲁棒的全局最大值和最小值。
  4. 鲁棒范围 `r(x) = max_avg - min_avg`。
- **优势**：避免了对孤立离群值的敏感性，且无需排序操作（复杂度 O(N·C)），仅需一次计算。
- **后处理**：将 `r(x)` 裁剪至 `[r_min, r_max]`（由校准集0.1%/99.9%分位数确定）。

#### ② Taylor-Motivated Polynomial Predictor
- **思想**：假设最优尺度 `s*_l(x)` 与 `r(x)` 存在光滑非线性关系，对函数进行d阶泰勒展开，简化为多项式形式：
  ```math
  s_l(x) = Σ_{k=0}^{d} c_{l,k} · (r(x))^k
  ```
- **学习**：多项式系数 `c_{l,k}` 在PTQ校准阶段通过**块重建损失**（block-wise reconstruction loss）优化，使用直通估计器（STE）反向传播。
- **初始化**：常数项 `c_{l,0}` 初始化为静态最优尺度，高阶系数置零，保证初始表现与静态PTQ一致。
- **复杂度**：每个量化层仅需 `d+1` 个系数，推理时一次多项式求值（O(1) FLOPs）。

### 2.3 推理流程
- 输入单张图像 → 计算 Patch Embedding → 提取鲁棒范围 `r(x)` → 裁剪 → 对于每个量化层 `l`，用多项式预测尺度 `s_l(x)` → 使用该尺度执行该层的量化前向传播。

# 3. 实验设计：数据集、Benchmark、对比方法

- **数据集**：ImageNet（约120万训练图像，5万验证图像，1000类）。
- **评估指标**：Top-1准确率（%）。
- **模型架构**：7种代表性ViT：ViT-Small、ViT-Base、DeiT-Tiny、DeiT-Small、DeiT-Base、Swin-Small、Swin-Base。
- **基准方法（Baselines）**：
  - **经典PTQ**：BRECQ、QDrop。
  - **ViT专用PTQ**：AdaLog、I&S-ViT。
  - **动态量化对比**：JIT Min/Max（运行时全张量min/max）、JIT Percentile（运行时排序后取分位数）。
- **比特配置**：低比特重点测试 W2/A3、W3/A3、W4/A3（权重位宽/激活位宽）。
- **实现细节**：
  - 校准集：从训练集随机采样1024张图像。
  - 对称均匀量化权重；激活量化依据各基线（Log-√2、AdaLog或均匀量化）。
  - 鲁棒范围提取：K=32个块。
  - 多项式度数d在实验中探索1~4，默认d≤2。
  - 块重建损失逐块优化，使用STE。

# 4. 资源与算力

- **文中明确提及**：所有实验在 **NVIDIA GeForce RTX 3090 GPU** 上使用PyTorch进行。
- **未明确说明**：具体的训练时长、GPU数量、总计算量（如GPU·时）。仅提到校准集大小1024张，优化过程为“block by block”重建，未给出完整训练时间。因此无法量化总算力消耗。
- **推理开销分析**：通过C++/CUDA核函数优化，在RTX 3090上测试了端到端延迟（表4），ScalePredictor比静态PTQ仅增加 **+0.4%～0.8%** 的延迟。

# 5. 实验数量与充分性

### 数量统计
- **主实验（表1）**：覆盖7种架构 × 3种比特配置 × 4种PTQ基线（每种有静态基线 和 +Ours两行）+ 2种JIT动态量化方法，共计超过 **84个精度结果**。
- **消融实验（图4、表2、表3）**：详细研究了：
  - 预测器输入（范围、均值、方差、峰度及其组合）→ 表2
  - 预测器函数（多项式 vs. MLP）→ 表3
  - 范围提取位置（各层）→ 图4a
  - 块大小K与多项式度数d的联合影响 → 图4b
- **效率分析（表4-6）**：端到端延迟、尺度计算开销、范围提取延迟的对比（每项3组batch size）。

### 充分性与公平性
- **充分性**：实验覆盖了主流ViT架构、多种量化方法、超参数调优，消融设计合理，基本回答了所有设计选择的影响。
- **公平性**：所有对比均在相同实验设置下进行，仅替换量化尺度生成方式；JIT动态量化同样在同一硬件上测试。初始化和损失函数的设置确保了公平对比。
- **局限性**：仅使用ImageNet单一数据集，未在更大或更小规模数据集（如CIFAR、COCO）、其他任务（检测、分割）上验证。这限制了泛化性结论的力度。

# 6. 主要结论与发现

1. **浅层-深层尺度相关性**：首次揭示了ViT中Patch Embedding输出的鲁棒范围与后续各层最优量化尺度之间存在强且一致的相关性。
2. **ScalePredictor显著提升PTQ精度**：在W2/A3等极端低比特下，最高提升 **+10.58%**（I&S-ViT + Swin-Small）；在W3/A3和W4/A3上也一致改进。
3. **效率媲美静态PTQ**：推理延迟仅增加0.6%，远低于JIT动态量化的19%~154%开销。
4. **多项式预测器优于MLP**：多项式在精度和效率上均优于单层MLP，且具有更优的归纳偏置，避免过拟合。
5. **鲁棒范围提取是关键**：使用块平均极值比使用全局min/max或分位数排序更鲁棒且高效。
6. **最佳配置**：预测器输入为鲁棒范围r，提取位置为Patch Embedding，块大小K=32，多项式度数d≤2。

# 7. 优点

- **新颖性**：首次将动态量化思想引入ViT的后训练量化，基于浅层-深层相关性设计，而非简单的逐层运行时计算。
- **方法实用性强**：多项式预测器仅需d+1个系数/层，推理无额外归约操作，硬件友好（O(1)计算）。
- **兼容性好**：可直接嵌入现有PTQ框架（BRECQ、QDrop、AdaLog、I&S-ViT），即插即用。
- **实验全面且深入**：覆盖多种架构、比特配置、基线，并进行了详尽的消融分析（输入、位置、块大小、多项式度数等），验证了各组件必要性。
- **效率验证充分**：不仅报告精度，还通过有/无优化内核的延迟测试定量说明极低开销。

# 8. 不足与局限

- **实验场景单一**：仅在ImageNet分类任务上验证，未在检测、分割、跟踪等下游任务或更小/更大数据集（如CIFAR-100、ADE20K）上测试，泛化性存疑。
- **部分基线出现少量退化**：QDrop + ScalePredictor在某些配置（如Swin-Small W2/A3）下精度比静态QDrop低约1.8%，虽然整体仍高于其他方法，但说明与某些校准策略的兼容性不完美。
- **预测器输入仅选鲁棒范围**：消融显示加入额外统计量（均值、方差等）反而可能降低性能，但未探索非线性组合（如相乘）或更复杂特征工程，可能遗漏更优输入表达。
- **多项式度数选取依赖手工**：d≥3容易过拟合（尤其校准集仅1024张），但未提出自动选择度数的规则。
- **校准开销未详细报告**：文中未给出实际的校准训练时间（如多少epoch、多少GPU小时），难以全面评估训练成本。
- **硬件测试局限性**：仅测试了单张RTX 3090，未在移动端/边缘端（如Jetson、ARM CPU）验证实际延迟，无法直接推断边缘部署的实用性。

（完）
