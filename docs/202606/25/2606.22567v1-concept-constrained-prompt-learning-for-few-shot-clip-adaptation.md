---
title: Concept-Constrained Prompt Learning for Few-Shot CLIP Adaptation
title_zh: 概念约束的提示学习用于少样本CLIP自适应
authors: "Na Sang, Ding Ma, Rui Sang, Yuxuan Liu"
date: 2026-06-21
pdf: "https://arxiv.org/pdf/2606.22567v1"
tags: ["query:vit"]
score: 9.0
evidence: 带概念约束的少样本提示学习用于CLIP适配
tldr: 少样本提示学习存在基类过拟合问题。本文提出概念约束提示学习（CCPL），将可学习类提示锚定到冻结的概念原型，仅需少量文本正则化。在DTD和EuroSAT上提升基类到新类调和平均，OxfordPets持平。为CLIP适应提供轻量有效方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.22567v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1558, \"height\": 881, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22567v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1534, \"height\": 509, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22567v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1238, \"height\": 647, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22567v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1503, \"height\": 500, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22567v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1367, \"height\": 752, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.22567v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1045, \"height\": 219, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22567v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 637, \"height\": 366, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22567v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 889, \"height\": 316, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22567v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 703, \"height\": 295, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22567v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1720, \"height\": 774, \"label\": \"Table\"}]"
motivation: 仅类别提示优化易过拟合基类，削弱对未见类的迁移能力。
method: CCPL：学习共享上下文令牌，用余弦一致性约束类提示嵌入与冻结概念原型，辅以概念丢弃正则化。
result: 在DTD和EuroSAT上基类到新类调和平均提升0.6和2.9，OxfordPets仅降0.1。
conclusion: 概念约束在概念原型与数据集语义对齐时最有效，细粒度类别是当前边界。
---

## 摘要
少样本提示学习是将CLIP适应到下游任务的有效策略，但仅对类别提示进行优化可能会过度拟合基类监督，并削弱对未见类别的迁移。我们提出了概念约束的提示学习（CCPL），这是一种轻量级正则化框架，在不更新CLIP编码器的情况下，将可学习的类别提示锚定在冻结的概念级文本原型上。CCPL学习一组共享的上下文标记，通过附加类别名称实例化类别提示，并从类别级概念库构建冻结的概念原型。在训练期间，文本空间的余弦一致性目标将可学习的类别提示嵌入与冻结的概念原型对齐；概念丢弃提供了额外的正则化，以防止对固定概念列表的过度依赖。在推理时，CCPL可选地使用可控集成权重alpha融合类别提示logits与概念原型logits。我们的默认配置使用文本空间概念正则化lambda=0.5、概念丢弃p=0.3和弱概念引导融合（alpha=0.1），且无基于KL的预测一致性项。在相同的自动生成回退划分下的实验表明，与CoOp相比，CCPL在DTD（+0.6）和EuroSAT（+2.9）上提高了基类到新类的调和平均值，而在OxfordPets（-0.1）上保持接近中性。消融实验表明，文本空间概念正则化始终有益，而最佳的概念引导推理强度对数据集和协议敏感。这些结果表明，当概念原型与数据集语义自然对齐时，概念约束最为有效，并识别出细粒度类别为当前的边界条件。代码发布在：https://github.com/richael-sang/concept-constrained-prompt-learning。

## Abstract
Few-shot prompt learning is an effective strategy for adapting CLIP to downstream tasks, but class-only prompt optimization can overfit base-class supervision and weaken transfer to unseen classes. We propose Concept-Constrained Prompt Learning (CCPL), a lightweight regularization framework that anchors learnable class prompts to frozen concept-level text prototypes without updating CLIP encoders. CCPL learns a set of shared context tokens, instantiates class prompts by appending class names, and constructs frozen concept prototypes from a class-level concept bank. During training, a text-space cosine consistency objective aligns learnable class-prompt embeddings with frozen concept prototypes; concept dropout provides additional regularization against over-reliance on fixed concept lists. At inference, CCPL optionally fuses class-prompt logits with concept-prototype logits using a controllable ensemble weight alpha. Our default configuration uses text-space concept regularization lambda = 0.5, concept dropout p = 0.3 and weak concept-guided fusion (alpha = 0.1), with no KL-based prediction consistency term. Experiments under identical automatically-generated fallback splits show that CCPL improves the base-to-new harmonic mean on DTD (+0.6) and EuroSAT (+2.9) compared with CoOp, while remaining near-neutral on OxfordPets (-0.1). Ablations indicate that text-space concept regularization is consistently beneficial, while the best concept-guided inference strength is dataset- and protocol-sensitive. These results suggest concept constraints are most effective when concept prototypes align naturally with dataset semantics, and identify fine-grained categories as a current boundary condition. The code is released at: https://github.com/richael-sang/concept-constrained-prompt-learning.

---

## 论文详细总结（自动生成）

# 概念约束的提示学习用于少样本CLIP自适应（CCPL）——详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：少样本提示学习（如CoOp）虽然能高效地将CLIP适配到下游任务，但仅针对基类（base classes）优化可学习的上下文令牌（prompt context tokens）容易导致过拟合，削弱对未见类（new classes）的泛化能力。CLIP的零样本能力依赖于视觉特征与自然语言描述的丰富对齐，而简单类名语义稀疏，学习到的提示可能偏离CLIP原本编码的富含语义的方向。
- **整体含义**：作者提出通过引入类级别的概念短语（concept phrases）作为冻结的文本空间锚点（text-space anchors）来正则化提示学习，使可学习提示保持在CLIP语义丰富的方向附近，从而在不增加额外可训练参数的前提下，改善基类到新类的迁移性能。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：概念约束的提示学习（CCPL）在CoOp框架基础上增加一个轻量级的文本空间正则化项。构造冻结的概念原型（concept prototypes）——每个类一组概念短语的文本嵌入平均值。训练时通过余弦一致性损失（cosine consistency loss）将可学习的类提示嵌入拉向对应的概念原型，同时使用概念丢弃（concept dropout）增加鲁棒性。推理时可选择性地融合类提示logits与概念原型logits（融合权重alpha可控）。
- **关键技术细节**：
  - **类提示参数化**：学习M个共享上下文令牌，与类名拼接形成提示，通过冻结的CLIP文本编码器得到归一化嵌入。
  - **概念原型构建**：为每个类定义K_c个概念短语（从手写概念库或模板生成），用冻结文本编码器计算每个短语的归一化嵌入后取平均，再归一化得到原型。概念库覆盖所有类（包括基类和新类），但训练时仅对基类计算正则化损失。
  - **概念丢弃**：训练时以概率1-p_drop独立保留每个概念短语；若全部丢弃则强制保留一个。丢弃掩码后的原型用于损失计算。默认p_drop=0.3。
  - **训练目标**：L_CCPL = L_ce + lambda_text * L_text，其中L_text为基类余弦一致性损失的均值（1 - cos(类提示嵌入, 概念原型)）。lambda_text默认0.5。
  - **概念引导推理**：最终logits = (1-alpha)*类提示logits + alpha*概念原型logits。alpha默认0.1。
  - **伪代码算法1**描述了完整训练流程：预计算所有类的概念原型；每步前向计算类提示嵌入、应用概念丢弃、计算L_ce和L_text，仅更新共享上下文令牌θ（CLIP编码器冻结）。推理时使用完整概念集（无丢弃）并可选融合。
- **无KL项**：默认方法仅包含交叉熵和文本正则化，不包含预测级的KL散度一致性项（初步实验发现不稳定）。

## 3. 实验设计

- **数据集与场景**：三类视觉识别挑战：
  - DTD（纹理识别，47类，base-to-new协议）
  - EuroSAT（卫星场景分类，10类，base-to-new协议）
  - OxfordPets（细粒度宠物品种分类，37类，base-to-new协议）
- **Benchmark**：Base-to-new协议（在基类上4-shot训练，在基类和新类上评估），报告基类准确率、新类准确率和调和平均H。
- **对比方法**：主要对比CoOp（最基础的提示学习基线）。未与其他最新方法（如Co-CoOp、MaPLe、PromptSRC、KgCoOp、ProGrad等）进行直接比较（论文认为未来工作可补充）。
- **实现细节**：CLIP ViT-B/16，冻结编码器；16个上下文令牌（共享）；SGD优化器，lr=0.002，cosine调度，warmup 1 epoch，batch size 32；训练50 epochs。

## 4. 资源与算力

- 论文在实验部分提到：“All training and evaluation were performed on NVIDIA RTX 4090 GPUs.” 但未给出具体数量、训练总时长等细节。仅说明单卡或有限GPU环境，未提供总计算量估计。

## 5. 实验数量与充分性

- **主要实验**：在三个数据集上进行主结果（表2），每个数据集一个种子（seed=1）的base-to-new结果，其中DTD额外有一个seed=2的鲁棒性证据（表3）。
- **消融实验**：仅在EuroSAT上进行了完整的消融（表4，图4）：移除文本正则化、移除推理集成、增强集成，以及不同alpha值的灵敏度分析（图5）。其他数据集未做消融。
- **额外支持实验**：DTD和EuroSAT的all-class结果（种子1和2）作为补充。
- **充分性评估**：实验覆盖有限——仅3个数据集，且仅对比CoOp一个基线，种子数少（EuroSAT和OxfordPets仅一个种子）。缺乏与其他SOTA方法（Co-CoOp等）的对比，也未在官方划分（Zhou et al.官方splits）上验证。因此实验充分性不足，结论的泛化性受限。消融仅在EuroSAT上进行，其他数据集未能验证正则化各组件的影响。

## 6. 论文的主要结论与发现

- **主结果**：CCPL-default相比CoOp在DTD上调和平均提升0.6（62.6→63.2），在EuroSAT上提升2.9（66.0→68.9，主要来自新类准确率提升4.3个百分点），在OxfordPets上几乎持平（96.3→96.2，微降0.1）。
- **概念约束有效条件**：当概念短语能捕捉数据集显著视觉属性时效果最好（如EuroSAT的场景级属性）；对于细粒度数据集（OxfordPets），通用模板概念不提供有信息量的约束，甚至可能略微有害。
- **消融发现**：文本空间正则化贡献积极（移除后H下降1.3）；概念引导推理在alpha=0.1时可额外提升（无集成时H接近CoOp）；alpha控制基类-新类权衡（更大alpha提升新类准确率但降低基类准确率）。
- **边界条件**：细粒度分类是当前方法的短板；概念原型质量至关重要。

## 7. 优点：方法或实验设计上的亮点

- **轻量高效**：不增加可训练参数（除CoOp已有的共享上下文令牌外），计算开销极小（仅一次额外余弦相似度计算），推理时可选融合。
- **概念可解释性**：使用人类可理解的概念短语作为锚点，提供语义正则化方向，有助于理解提示学习为何改进/退化。
- **灵活可控的推理融合**：通过alpha参数在测试时调整对概念原型的依赖，无需重新训练。
- **概念丢弃正则化**：增强对固定概念列表的鲁棒性，防止过度依赖特定短语。
- **坦诚报告局限性**：明确说明使用回退划分而非官方划分、仅对比CoOp、概念库为手工制作、种子数量有限等，并给出边界条件分析，态度诚实。

## 8. 不足与局限

- **实验覆盖不足**：
  - 仅3个数据集，且仅EuroSAT做了完整消融。缺乏对更多数据集（如ImageNet、Caltech101、Flowers等）的验证。
  - 仅对比CoOp，未与Co-CoOp、MaPLe、PromptSRC、KgCoOp、ProGrad等主流方法比较，无法证明CCPL优于当前SOTA。
  - 有效种子数极少（EuroSAT和OxfordPets仅一个种子），鲁棒性证据薄弱。
- **使用非官方划分**：采用自动生成的回退划分，导致绝对数值无法与已发表论文直接比较，削弱了结果的通用性。
- **概念库依赖人工/模板**：EuroSAT的手写概念需要人工努力；DTD和OxfordPets的模板概念可能不够丰富或有误导性（如“Siamese cat texture”无意义）。未探索LLM自动生成。
- **未测量概念对齐**：未直接验证训练过程中类提示嵌入与概念原型对齐程度的变化，缺乏机制层面的诊断。
- **细粒度场景效果不佳**：方法在OxfordPets上几乎无效，对细粒度分类的适用性有待提升。
- **潜在偏差风险**：默认超参数（lambda=0.5, p=0.3, alpha=0.1）可能仅在EuroSAT上调优良好，在其他数据集上未必最优。

（完）
