---
title: "SPAR: Semantic-Pixel Self-Alignment and Adaptive Routing for Unified Multimodal Models"
title_zh: SPAR：面向统一多模态模型的语义-像素自对齐与自适应路由
authors: "Hongxiang Li, Hongxu Chen, Chenyang Zhu, Xiaoshuang Huang, Jiayin Cai, Xiaolong Jiang, Yao Hu, Long Chen"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.23041v1"
tags: ["query:cv-lm"]
score: 8.0
evidence: 多模态大语言模型弥合语义感知与像素重建差距
tldr: 针对多模态大模型在视觉生成中感知与重建特征不一致的问题，提出SPAR框架。通过非对称双流统一分词器协调语义与像素级重建，并利用自对齐范式消除外部依赖。动态令牌路由使每个令牌自适应聚合多层MLLM特征。实验表明SPAR在统一架构中达到最优，生成与重建质量优异且保留视觉理解能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.23041v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1208, \"height\": 603, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23041v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1268, \"height\": 534, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23041v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1241, \"height\": 694, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23041v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1263, \"height\": 1247, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.23041v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 815, \"height\": 751, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23041v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1257, \"height\": 827, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23041v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1222, \"height\": 848, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23041v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1221, \"height\": 590, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23041v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1198, \"height\": 300, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23041v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 957, \"height\": 221, \"label\": \"Table\"}]"
motivation: 弥合语义感知与像素级重建之间的特征差异，实现统一的生成与理解模型。
method: 提出非对称双流分词器（语义流+Transformer增强像素流）和自对齐生成范式，并引入动态令牌路由。
result: 在统一架构中达到最优生成与重建质量，同时保持基础视觉理解能力。
conclusion: SPAR有效统一了多模态模型的生成与理解能力，无需外部教师模型。
---

## 摘要
多模态大语言模型在视觉理解方面取得了显著成功，但由于语义感知与像素级重建之间的根本特征差异，在视觉生成方面仍然受限。弥合这一差距需要克服两个核心挑战：赋予语义编码器高保真重建能力，以及在不依赖外部教师的情况下有效地将生成模型与语义空间对齐。为此，我们提出了一种新颖的统一多模态框架，该框架具有语义-像素自对齐与自适应路由（SPAR）。首先，为了协调语义感知与像素级重建，我们引入了一种非对称双流统一分词器。轻量级语义流锚定判别性特征，而Transformer增强的像素流将细粒度视觉细节恢复为统一的紧凑潜在空间。其次，为了消除外部依赖，我们提出了一种自对齐生成范式，该范式天然地将该优化后的分词器作为扩散模型的内部对齐教师。此外，为了促进该统一空间内的灵活多模态交互，我们引入了动态令牌路由，使得每个令牌能够根据其不同的语义需求自适应地聚合多层多模态大语言模型特征。大量实验表明，SPAR确立了统一架构的最先进水平，在保持基础视觉理解能力的同时，实现了卓越的生成和重建质量。

## Abstract
Multimodal Large Language Models (MLLMs) have achieved remarkable success in visual understanding but remain constrained in visual generation due to the fundamental feature discrepancy between semantic perception and pixel-level reconstruction. Bridging this gap requires overcoming two core challenges: endowing semantic encoders with high-fidelity reconstruction capabilities, and effectively aligning generative models with semantic spaces without relying on external teachers. To this end, we propose a novel unified multimodal framework featuring \textbf{S}emantic-\textbf{P}ixel self-alignment and \textbf{A}daptive \textbf{R}outing (\textbf{SPAR}). First, to reconcile semantic perception with pixel-level reconstruction, we introduce an asymmetric dual-stream unified tokenizer. A lightweight semantic stream anchors discriminative features, while a Transformer-augmented pixel stream recovers fine-grained visual details into a unified compact latent space. Second, to eliminate external dependencies, we propose a self-aligned generation paradigm that natively leverages this optimized tokenizer as an internal alignment teacher for the diffusion model. Furthermore, to facilitate flexible multimodal interaction within this unified space, we introduce Dynamic Token Routing, which enables each token to adaptively aggregate multi-layer MLLM features based on its distinct semantic demands. Extensive experiments demonstrate that SPAR establishes the state-of-the-art for unified architectures, achieving exceptional generation and reconstruction quality while preserving foundational visual understanding capabilities.

---

## 论文详细总结（自动生成）

# 论文中文总结

## 1. 核心问题与整体含义（研究动机和背景）
- **核心问题**：多模态大语言模型（MLLMs）在视觉理解任务上表现出色，但在视觉生成方面受限于语义感知与像素级重建之间的根本性特征差异。具体表现为：语义编码器的特征空间是面向判别任务的高度抽象压缩，丢失了纹理、结构等像素级细节，导致直接在该空间上进行生成时重建质量差（模糊、失真）；而若将语义编码器完全微调以恢复细节，又会引发灾难性遗忘，破坏原有理解能力。
- **研究动机**：弥合语义感知与像素重建之间的鸿沟，实现统一的视觉理解与生成模型，同时避免依赖外部预训练特征网络（如DINOv2）作为对齐教师，以解决分布偏差和扩展性问题。
- **整体含义**：提出SPAR框架，通过非对称双流分词器实现语义保持与像素恢复的解耦，并自利用该分词器作为内部对齐教师，消除外部依赖，再辅以动态令牌路由灵活聚合MLLM层次特征，最终实现统一框架下的高质量生成与理解。

## 2. 方法论：核心思想、关键技术细节
### 核心思想
- **非对称双流统一分词器**：在冻结的语义编码器输出之上，构建轻量级语义流（保留判别特征）与Transformer增强的像素流（恢复高频细节），两流在紧凑潜在空间中融合，再经像素解码器重建图像。
- **自对齐生成范式**：将优化后的分词器本身作为扩散模型（DiT）的对齐教师，避免外部网络引入的分布偏差。
- **动态令牌路由（DTR）**：使每个令牌（token）能够根据自身语义角色自适应地选择MLLM多层特征的融合权重，充分利用层次化表示。

### 关键技术细节
1. **双流水线**：
   - 语义流：少量残差块+MLP，将语义特征 \(f_v\) 映射到紧凑潜在空间 \(z_s\)，保持原始判别结构。
   - 像素流：Transformer编码器处理 \(f_v\) 后经残差块+MLP得到 \(z_p\)，并与像素编码器真值对齐（像素对齐损失 \(L_p\)）。
   - 融合：通道拼接后经融合层映射回潜在空间，送入预训练像素解码器重建。
2. **渐进式三阶段训练**：
   - 阶段I：冻结编码器/解码器，只训练双流模块，使用MSE、LPIPS和像素对齐损失。
   - 阶段II：冻结编码器，联合训练像素解码器，仅使用重建损失。
   - 阶段III：微调编码器，引入语义对齐损失（与冻结编码器输出余弦相似度）和GAN损失，防止灾难性遗忘。
3. **条件信号构建**：在MLLM输入中插入可学习查询嵌入作为目标图像占位符，通过因果自注意力吸收上下文。
4. **动态令牌路由**：
   - 从MLLM的L层中均匀采样K层隐藏状态 \(H^{(k)}\)，以最深层特征作为查询，通过轻量路由网络计算软权重 \(w_i = \text{softmax}(g(H_i^{(K)})/\tau)\)。
   - 逐层缩放后加权求和，再经线性投影得到最终条件特征。
5. **自对齐损失**：对齐DiT中间层特征与分词器编码器投影后的特征，采用余弦相似度最大化。

## 3. 实验设计
### 数据集
- **生成任务**：BLIP3o公开的27M重新标注数据 + CC12M的5M子集 + JourneyDB的4M合成图像。
- **编辑任务**：GPT-Image-Edit数据集。
- **SFT阶段**：BLIP3o-60K + ShareGPT-4o-Image高质量数据。
- **理解任务**：无需额外数据（冻结LLM，仅替换视觉编码器）。
- **评测基准**：
  - 重建：ImageNet 50k验证集（256×256）。
  - 理解：MME-P、MMB、MMMU、MM-Vet、SEED、MMVP。
  - 文本生成图像：GenEval、WISE。
  - 图像编辑：ImgEdit-Bench。

### 对比方法
- **重建**：LlamaGen、VAR、Open-MAGVIT2、RAE、SD-VAE、DC-AE、VA-VAE 等生成式分词器；VILA-U、TokenFlow、DualViTok、DualToken、EMU2、UniLIP 等统一分词器。
- **理解**：纯理解模型（LLaVA-OV、InternVL3、Qwen2.5-VL、Emu3-Chat）和统一模型（Chameleon、VILA-U、MetaMorph、SEED-X、Show-O、Janus-Pro、Harmon、BAGEL、BLIP3-o、TokLIP、Tar）。
- **生成**：纯生成模型（SDXL、FLUX.1-dev、Emu3-Gen、SD3-Medium、Sana-1.6B）和统一模型（VILA-U、TokenFlow-XL、Janus-Pro、Harmon、BLIP3-o、BAGEL、OpenUni、Show-o2、Tar）。
- **编辑**：GPT-4o、MagicBrush、Instruct-P2P、AnyEdit、UltraEdit、OmniGen、Step1X-Edit、ICEdit、BAGEL、UniWorld-V1、OmniGen2。

### 模型变体
- SPAR-1B：MLLM=InternVL3-1B，DiT=SANA-0.6B。
- SPAR-3B：MLLM=InternVL3-2B，DiT=SANA-1.6B。

## 4. 资源与算力
论文**未明确说明**使用的具体GPU型号、数量、训练时长等算力信息。仅提及训练数据规模（约36M图像），模型参数量（1B/3B级别），但未提供训练硬件配置或耗时数据。

## 5. 实验数量与充分性
- **实验数量**：共进行了六组主要实验（重建、理解、文本生成图像、图像编辑、分词器消融、统一模型消融），每组均报告多个指标。
- **充分性**：
  - 重建实验覆盖了rFID、PSNR、SSIM三个指标，对比了10种以上方法。
  - 理解实验覆盖6个主流基准，对比了多种规模的模型。
  - 生成实验覆盖两个基准（GenEval、WISE）及编辑基准，对比了10余种开放/闭源方法。
  - 消融实验：分别对分词器的像素流、语义流、冻结编码器三个变体进行分析；对统一模型的DTR和自对齐损失两个组件进行消融。
- **客观与公平性**：
  - 对比方法广泛，包含生成专用、统一模型及闭源模型。
  - 指标选择标准（rFID、PSNR、SSIM、MME等）均为领域通用。
  - 消融实验设计合理，验证了各组件的必要性。
  - 但缺少对更大规模（如7B级别）模型的对比，也缺少训练效率或推理速度的比较。

## 6. 主要结论与发现
- **重建质量**：SPAR在统一分词器中达到SOTA（rFID=0.27，PSNR=26.65，SSIM=0.856），甚至超越部分生成专用分词器（如VA-VAE）。
- **理解能力**：未因生成训练而退化，反而在多个理解基准上超越基线InternVL3（例如MME-P从1492提至1500，MMVP从67.3提至68.9）。
- **生成质量**：SPAR-3B在GenEval上总分0.91（计数0.84、位置0.87），超过所有对比方法（包括12B的FLUX.1-dev）；在WISE上总分0.64，同样领先。
- **图像编辑**：SPAR-3B在ImgEdit-Bench上总体4.01，仅低于GPT-4o（4.20），远超其他开源方法（如OmniGen2为3.44）。
- **自对齐与动态路由**：消融实验证明，去除DTR或自对齐损失均导致性能下降，验证了其有效性。
- **关键发现**：非对称双流设计能有效解耦语义保持与像素重建，自对齐范式可消除外部教师依赖，动态路由能更灵活利用MLLM层次特征。

## 7. 优点
- **方法创新**：
  - 提出非对称双流分词器，显式分离语义与像素任务，避免信息冲突。
  - 自对齐生成范式将分词器自身作为内部教师，消除外部依赖，减少分布偏差。
  - 动态令牌路由提供自适应多层级特征聚合，提升生成条件质量。
- **实验全面**：覆盖重建、理解、生成、编辑四大任务，对比方法丰富，消融设计合理。
- **性能突出**：在多个基准达到SOTA，且模型规模较小（1B/3B），表明方法高效。
- **理解能力无损甚至提升**：解决了统一模型中常见的理解退化问题。

## 8. 不足与局限
- **算力信息缺失**：未提供训练所需GPU型号、数量、时长，难以评估复现成本。
- **模型规模限制**：实验仅包含1B和3B参数两个变体，未探索更大容量（如7B以上）的表现，可能限制泛化结论。
- **数据依赖**：训练数据主要来自公开数据集，未详细说明数据清洗或去重过程，存在潜在的数据偏差风险。
- **编辑任务对比**：仅使用ImgEdit-Bench一个基准，覆盖场景有限（9类），且未包含“风格迁移”或“物体移除”等常见细分类别的单独分析。
- **理解任务不完整**：虽然评估了6个基准，但缺少对细粒度视觉定位、视频理解等任务的验证。
- **动态路由解释性**：虽提及可视化路由权重可提供解释性，但论文未展示具体分析结果。
- **推理效率未报告**：未提供生成速度、内存占用等实际部署指标。

（完）
